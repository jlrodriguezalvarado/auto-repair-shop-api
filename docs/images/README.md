# Screenshot placeholders

Capture these manually for the GitHub README / portfolio. Do **not** commit fabricated image binaries.

| Suggested file | What to capture |
|----------------|-----------------|
| `swagger-ui.png` | Swagger UI at `/api/schema/swagger-ui/` (authenticated or public schema view) |
| `architecture.png` | Export of the Mermaid architecture diagram from the README (or draw.io equivalent) |
| `dashboard-summary.png` | Dashboard / summary response in Swagger or a tenant admin UI consuming `/api/dashboard/summary/` |
| `frontend-consuming-api.png` | Angular app (`auto-repair-shop-front`) calling this API (network panel or feature screen) |

Optional extras:

| Suggested file | What to capture |
|----------------|-----------------|
| `redoc.png` | ReDoc at `/api/schema/redoc/` |
| `work-order-flow.png` | Work order detail or status transition in the UI |
| `ci-green.png` | GitHub Actions CI run passing on a PR |

Store finished PNGs/WebPs in this folder and link them from the root README when ready.
