# Local: auto-repair-shop-api/deploy/.env.deploy (created/updated by build-image.sh).
# Server: /opt/apps/mechanics/.env.deploy
# APP_VERSION → registry…/mechanics/api:<APP_VERSION>
# WEB_VERSION → registry…/mechanics/web:<WEB_VERSION>
APP_VERSION=replace-me
WEB_VERSION=replace-me
