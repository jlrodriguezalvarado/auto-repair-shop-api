# Dated feature records (auto-repair-shop-api)

```text
YYYY-MM-DD-<short-kebab-case-slug>.md
```

Date in `America/Caracas`. Start from `TEMPLATE.md`.

This directory holds **API-only** and **cross** (API + front) approved plans. Cross plans should set `Repositories: both` (or equivalent Scope: cross). Frontend-only plans belong in `../auto-repair-shop-front/.plans/`.

Do not silently rewrite approved scope after development begins; append amendments and the final QA result to the same file.
